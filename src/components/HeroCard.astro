---
import { Icon } from "astro-icon/components";

const { number, icon } = Astro.props;
---

<article>
  <Icon name={icon} />

  <div>
    <p class="big">{number} +</p>
    <p>
      <slot />
    </p>
  </div>
</article>

<style>
  article {
    background: var(--beige);
    border-radius: var(--radius-s);
    padding: var(--space-xs) var(--space-m);
    flex-grow: 1;

    display: flex;
    gap: var(--space-s);
    align-items: center;

    [data-icon] {
      max-width: 4rem;
    }

    div:has(p) {
      color: var(--purple);
    }
  }

  .big {
    font-size: var(--step-2);
  }
</style>
