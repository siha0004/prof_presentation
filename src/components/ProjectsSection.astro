---
import { Icon } from "astro-icon/components";

import ScrollingContainer from "../components/ScrollingContainer.astro";
import Text from "../components/Text.astro";

const { data } = Astro.props;
console.log(data);
---

<section class="full-bleed">
  <Text heading="Projekter" size="h2">
    <p>Som studerende på multimediedesign, har jeg arbejdet med digitalt design i mange afskygninger, men særligt med interesse indenfor designsystemer, UX/UI design og implementering af disse.</p>
    <p>Ved siden af mit studie er jeg co-founder af start-up’en Peer Performance. Her bidrager jeg særligt med min viden om UI/UX, designer interfacet til vores app, samt med designet og implementeringen af vores <a href="https://peer-performance.com/">hjemmeside</a>.</p>
  </Text>
  <ScrollingContainer cardData={data} />
  <div class="notes">
    <p class="note">Et udsnit af mit arbejde</p>
    <Icon name="arrow_1" />
  </div>
</section>

<style>
  section {
    grid-column: full;
    display: grid;
    grid-template-columns: inherit;
    row-gap: var(--space-xl);
    position: relative;

    .notes {
      display: none;
    }
  }
  @media (min-width: 900px) {
    section {
      small {
        display: none;
      }

      .notes {
        position: absolute;
        right: 2%;
        top: 160px;
        rotate: 15deg;
        z-index: 1;
        display: flex;
        flex-direction: column;
        gap: 1rem;
        [data-icon] {
          max-width: 110px;
          rotate: -15deg;
        }
      }
    }
  }
</style>
